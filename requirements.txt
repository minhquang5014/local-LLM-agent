# Core LangChain / LangGraph
langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0

# Vector memory
chromadb>=0.5.0

# Local LLM — Apple Silicon (primary backend)
# Install mlx-lm for fast native inference:
#   pip install mlx-lm
mlx-lm>=0.19.0

# Local LLM — HuggingFace transformers (fallback)
transformers>=4.45.0
accelerate>=0.34.0
torch>=2.4.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# Tools — web search
# duckduckgo-search was renamed to ddgs in version 7.x
# Install whichever is available; tools.py handles both imports:
ddgs>=7.0.0

# Tools — web scraping + HTTP
requests>=2.31.0
beautifulsoup4>=4.12.0

# Utilities
python-dotenv>=1.0.0
rich>=13.0.0
